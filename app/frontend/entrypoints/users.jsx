import React from 'react';
import ReactDOM from 'react-dom/client';

const container = document.getElementById('users');
const root = ReactDOM.createRoot(container);

root.render(<h1>hi I'm Paul</h1>);