require 'rails_helper'

RSpec.describe "Register User", driver: :selenium_chrome, js: true do 
  context "As a visitor" do 
    it "when visit the root path a see a button to register as a new user" do
    
    end
  end
end